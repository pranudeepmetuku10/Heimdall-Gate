# Runtime dependencies for the ingestion service.
# Spark/Delta deps are provided by the Databricks runtime and are NOT pinned here.
#
# Versions below are pinned to releases that ship prebuilt wheels for CPython
# 3.11-3.13 (incl. macOS arm64). In particular confluent-kafka bundles
# librdkafka in its wheel, so no system C library is required. Verified on
# Python 3.13.

confluent-kafka==2.14.2
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.1
tenacity==9.1.4
python-dotenv==1.2.2
orjson==3.11.9
